#!/bin/bash
set -exu
trap "docker-compose logs --no-color ;docker-compose kill" SIGINT SIGTERM EXIT
docker-compose kill
docker-compose build
docker-compose up -d redistools
docker-compose run redistools -e CI=true './bin/lint'